import signatureImg from "@/assets/signature-white.png";

const SignatureAnimation = () => {
  return (
    <img
      src={signatureImg}
      alt="Real Estate with Victoria"
      className="w-full h-auto max-w-4xl animate-fade-in"
    />
  );
};

export default SignatureAnimation;
